---
id: 0000
title: <short imperative phrase>
filed: YYYY-MM-DD
author: <name the context — auditor slug, task slug, or filer>
---

## Problem

What's the friction or gap, in 2-5 sentences. Concrete is better than
abstract — name the command, the file, the situation that prompted it.

State the friction, not a solution shape. Swap-test: if replacing your
intended fix with a structurally different one would make this paragraph
false, you've over-specified — the framing has silently pruned the
solution space before triage looks.

## Discussion

<!--
The CLI owns this section. First bullet is auto-emitted by `issue new`:
  - YYYY-MM-DD: filed via system-registry CLI (author: <name>).
Subsequent bullets land via `issue record` (verdict mode for triage,
comment mode for non-verdict notes) or `issue close` / `issue defer`.

Never hand-author the first bullet here — `issue new` writes it. The
section appears empty in this template because the live shape is
mechanism-set, not author-set.
-->

<!--
Open issues live here as NNNN-slug.md. Closed issues move to
issues/closed/NNNN-slug.md verbatim. Deferred issues to
issues/deferred/NNNN-slug.md. IDs are monotonic across all
three (skip closed and deferred, never reuse).
-->
